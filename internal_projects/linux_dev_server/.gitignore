# Secrets
.env

# SSH public keys (contain client identities)
ssh-keys/*.pub

# Persistent data directories (managed by Docker)
gitea/data/
buildbuddy/data/

# GitHub CLI auth token (persisted via bind mount)
gh-config/

# Tailscale state (node key, auth — persist across rebuilds)
tailscale-state/

# macOS
.DS_Store
