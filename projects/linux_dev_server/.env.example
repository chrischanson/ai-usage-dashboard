# ── API Keys ──────────────────────────────────────────────────
# Required: OpenRouter API key for AI coding agents
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Default model for Claude Code (OpenRouter ID)
ANTHROPIC_MODEL=anthropic/claude-3.5-sonnet

# Optional: OpenCode Zen API key (leave blank if not using)
OPENCODE_ZEN_API_KEY=

# Optional: Codex CLI — by default it reuses OPENROUTER_API_KEY via OPENAI_API_KEY.
# To use a separate OpenAI key for Codex, set CODEX_API_KEY below.
# To point Codex at real OpenAI instead of OpenRouter, set CODEX_OPENAI_BASE_URL.
# NOTE: If you set CODEX_OPENAI_BASE_URL=https://api.openai.com/v1, you MUST also
# set CODEX_API_KEY to a real OpenAI key (OPENROUTER_API_KEY won't work there).
CODEX_API_KEY=
CODEX_OPENAI_BASE_URL=

# ── Gitea Configuration ──────────────────────────────────────
# Admin account created automatically on first run
GITEA_ADMIN_USER=dev
GITEA_ADMIN_PASSWORD=changeme123
GITEA_ADMIN_EMAIL=dev@localhost

# ── Git Identity (used inside the dev container) ─────────────
GIT_USER_NAME=Dev User
GIT_USER_EMAIL=dev@localhost
